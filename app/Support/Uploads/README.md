# Support/Uploads

Ponto de extensão para regras de upload de arquivos clínicos (validação,
nomeação, armazenamento no disco `s3`/MinIO). Ainda sem implementação — o
disco `s3` já está configurado e testável (ver `docs/architecture/docker.md`).
